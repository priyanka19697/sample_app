- provide(:title, @user.name)
.row
  .col-md-4
  %aside.user_info
    %h1
      = gravatar_for @user
      = @user.name
  .col-md-8
  - if @user.microposts.any?
    %h3
      Microposts (#{@user.microposts.count})
    %ol.microposts
      = render @microposts
    = will_paginate @microposts


    -# render @microposts will render _micropost.html for each micropost it has and send id default.
 


